# The URL the site will be built for
base_url = "https://dadyarri.ru"
title = "Личный блог dadyarri"
description = "Мыслепомойка об IT и всяком вокруг"
author = "dadyarri"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# Whether to generate an rss feed
generate_feeds = true
feed_filenames = ["rss.xml"]

# Name of the used theme
theme = "yarri"

# Language
default_language = "ru"

taxonomies = [{ name = "tags" }, { name = "cycle" }]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
extra_syntaxes_and_themes = ["highlight_themes"]
highlight_theme = "tokyonight_moon"
render_emoji = true

[extra]
socials = [
    { name = "github", url = "https://github.com/dadyarri", icon = "github" },
    { name = "telegram", url = "https://t.me/dadyarri", icon = "telegram" },
]

menu = [
    { name = "/Посты", url = "/posts", weight = 1 },
    { name = "/Шпаргалки", url = "/minis", weight = 2 },
]

favicon = "/static/favicon.svg"
fancy_code = true
dynamic_note = true
toc = true
