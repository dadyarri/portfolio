---
import TagIndexPage from "@components/TagIndexPage.astro";
import { getTagIndexItems } from "@utils/content-routes";

const tags = await getTagIndexItems("minis");

---

<TagIndexPage items={tags} />
